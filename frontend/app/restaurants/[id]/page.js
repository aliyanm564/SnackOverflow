// Owner: Temi
// Feat3-FR1 — Restaurant detail page: show restaurant info + full menu
// Endpoint: GET /api/v1/restaurants/{id}
// Endpoint: GET /api/v1/restaurants/{id}/menu-items

export default function RestaurantDetailPage({ params }) {
  return (
    <main style={{ maxWidth: "800px", margin: "0 auto", padding: "2rem 1rem" }}>
      <p style={{ color: "#888" }}>Restaurant detail page — coming soon</p>
    </main>
  );
}
