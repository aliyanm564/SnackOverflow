// Owner: Muhammad
// Feat7-FR1, FR2, FR3 — Payment submission and result
// Endpoint: POST /api/v1/orders/{order_id}/pay
// Requires auth token in header

export default function PaymentPage() {
  return (
    <main style={{ maxWidth: "600px", margin: "0 auto", padding: "2rem 1rem" }}>
      <h1 style={{ marginBottom: "1.5rem" }}>Payment</h1>
      <p style={{ color: "#888" }}>Payment page — coming soon</p>
    </main>
  );
}
