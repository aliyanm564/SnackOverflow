// Owner: Aman + Temi
// Feat4-FR1 — Place order (Aman)
// Feat6-FR4 — Show price breakdown before confirming (Temi)
// Endpoint: GET /api/v1/pricing/quote  (price breakdown)
// Endpoint: POST /api/v1/orders        (place order)
// Requires auth token in header

export default function CheckoutPage() {
  return (
    <main style={{ maxWidth: "600px", margin: "0 auto", padding: "2rem 1rem" }}>
      <h1 style={{ marginBottom: "1.5rem" }}>Checkout</h1>
      <p style={{ color: "#888" }}>Checkout page — coming soon</p>
    </main>
  );
}
