// Owner: Muhammad
// Feat1-FR1 — Register form
// Endpoint: POST /api/v1/auth/register
// Fields: email, password, name, role (customer / restaurant_owner)

export default function RegisterPage() {
  return (
    <main style={{ maxWidth: "400px", margin: "4rem auto", padding: "0 1rem" }}>
      <h1 style={{ marginBottom: "1.5rem" }}>Create Account</h1>
      <p style={{ color: "#888" }}>Register form — coming soon</p>
    </main>
  );
}
