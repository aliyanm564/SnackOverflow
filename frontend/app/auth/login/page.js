// Owner: Muhammad
// Feat1-FR2 — Login form
// Endpoint: POST /api/v1/auth/login
// On success: store JWT token, redirect to /

export default function LoginPage() {
  return (
    <main style={{ maxWidth: "400px", margin: "4rem auto", padding: "0 1rem" }}>
      <h1 style={{ marginBottom: "1.5rem" }}>Login</h1>
      <p style={{ color: "#888" }}>Login form — coming soon</p>
    </main>
  );
}
