// Owner: Sukhi
// Feat2-FR4 — List restaurants owned by the logged-in user
// Endpoint: GET /api/v1/restaurants/owner/{owner_id}
// Requires auth token in header

export default function MyRestaurantsPage() {
  return (
    <main style={{ maxWidth: "800px", margin: "0 auto", padding: "2rem 1rem" }}>
      <h1 style={{ marginBottom: "1.5rem" }}>My Restaurants</h1>
      <p style={{ color: "#888" }}>Restaurant management — coming soon</p>
    </main>
  );
}
